# 聚玻明视 · AI 助手 前端实现文档

> 面向玻璃检测场景的 AI 助手 Web 前端，采用「玻璃拟态 + 深色主题」视觉风格，内置 DeepSeek / 玻璃智能体双后端，支持流式输出、思维链展示、图表看板与多会话管理。
>
> 本文档从「用到的技术」和「各功能的详细实现步骤」两个维度，逐模块说明前端实现。

---

## 目录

1. [技术栈总览](#1-技术栈总览)
2. [工程化配置](#2-工程化配置)
3. [应用启动流程](#3-应用启动流程)
4. [目录结构](#4-目录结构)
5. [基础能力层](#5-基础能力层)
6. [状态管理（Pinia）](#6-状态管理pinia)
7. [路由与布局](#7-路由与布局)
8. [侧边栏模块](#8-侧边栏模块)
9. [会话管理](#9-会话管理)
10. [消息发送与双后端流式](#10-消息发送与双后端流式)
11. [富文本渲染](#11-富文本渲染)
12. [图表与看板（ECharts）](#12-图表与看板echarts)
13. [滚动控制](#13-滚动控制)
14. [主题系统（玻璃拟态）](#14-主题系统玻璃拟态)
15. [移动端适配](#15-移动端适配)
16. [关键踩坑点与优化](#16-关键踩坑点与优化)
17. [本地存储键位](#17-本地存储键位)
18. [接口约定](#18-接口 约定)

---

## 1. 技术栈总览

| 分类 | 技术 | 版本 | 用途 |
|---|---|---|---|
| 框架 | Vue | ^3.5.13 | `<script setup>` 组合式 API |
| 语言 | TypeScript | ~5.7.2 | 全量类型约束 |
| 构建 | Vite | ^6.1.0 | 开发服务器、代理、打包 |
| 路由 | vue-router | 4 | 会话路由 `/chat/:uuid?` |
| 状态管理 | Pinia | ^3.0.1 | 应用状态、会话状态 |
| UI 组件库 | Element Plus | ^2.9.4 | 输入框、按钮、滚动条、弹窗 |
| 图标 | @element-plus/icons-vue | ^2.3.1 | 功能图标 |
| CSS | UnoCSS | ^65.5.0 | 原子化 class（`flex`、`w-320px` 等） |
| CSS 预处理器 | Sass | ^1.85.0 | 全局变量、嵌套样式 |
| 组合式工具 | @vueuse/core | ^12.7.0 | `useMediaQuery`、`onClickOutside` |
| Markdown | markdown-it | ^14.1.0 | AI 回复正文渲染 |
| 代码高亮 | highlight.js | ^11.11.1 | 代码块着色 |
| 数学公式 | katex + @vscode/markdown-it-katex | ^0.16.21 | LaTeX 公式渲染 |
| 流程图 | mermaid-it-markdown | ^1.0.8 | Mermaid 图表渲染 |
| 链接属性 | markdown-it-link-attributes | ^4.0.1 | 外链新窗口打开 |
| 可视化 | echarts | ^6.1.0 | 智能体返回的图表 / KPI 看板 |
| HTTP | axios | ^1.7.9 | 普通接口（登录等） |
| 流式 | 原生 fetch + ReadableStream | — | SSE 流式读取 |
| 多模态上传 | 原生 fetch + FormData | — | 图片 / 文档 multipart 上传 |

**自动导入插件（devDependencies）**：`unplugin-auto-import`（自动导入 API）、`unplugin-vue-components`（组件按需注册）、`autoprefixer`（前缀）。

---

## 2. 工程化配置

### 2.1 Vite 配置（`vite.config.ts`）

实现要点：

1. **路径别名**：`@` → `src`，`#` → `src/types`，扩展名补全 `.js/.ts/.jsx/.tsx/.json`。
2. **开发服务器**：端口 `3002`、`host: true`、`open: true`、`hmr: true`、`cors: true`。
3. **代理**：前端请求前缀 `/api`、`/agent` 由 Vite 转发并去掉前缀，解决跨域。

```ts
proxy: {
  '/api': {              // DeepSeek（OpenAI 兼容）网关
    target: viteEnv.VITE_APP_API_BASE_URL,
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, ''),
  },
  '/agent': {            // 玻璃检测智能体（无鉴权、独立地址）
    target: viteEnv.VITE_AGENT_API_BASE_URL,
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/agent/, ''),
  },
}
```

4. **自动按需引入**：`AutoImport` + `Components` 使用 `ElementPlusResolver`。
5. **产物分包（`build.rollupOptions.manualChunks`）**：把体积大、变更不频繁的第三方库拆成独立 chunk（`echarts` / `element-plus` / `markdown` / `vue`），便于浏览器长期缓存，二次刷新命中缓存、缩短白屏时间。

### 2.2 UnoCSS 配置（`uno.config.ts`）

```ts
export default defineConfig({
  presets: [presetUno({ dark: 'class' })],
})
```

`dark: 'class'` 表示暗色模式由 `<html>` 上的 `.dark` class 触发（配合主题系统）。

### 2.3 环境变量（`.env`）

| 变量 | 说明 |
|---|---|
| `VITE_APP_BASE_API` | 请求统一前缀（默认 `/api`） |
| `VITE_APP_API_BASE_URL` | DeepSeek 网关地址 |
| `VITE_APP_API_KEY` | DeepSeek 网关密钥（`Bearer` 下发） |
| `VITE_AGENT_API_BASE_URL` | 玻璃智能体服务地址 |
| `VITE_GLOB_OPEN_LONG_REPLY` | 长回复开关 |
| `VITE_GLOB_APP_PWA` | PWA 开关 |

---

## 3. 应用启动流程

`src/main.ts` 是入口，启动顺序如下：

```ts
async function bootstrapApp() {
  const app = createApp(App)
  initStore(app)              // 1. 注册 Pinia
  useAppStore().initTheme()   // 2. 应用本地存储的主题
  initRouter(app)             // 3. 注册路由
  app.mount('#app')           // 4. 挂载
}
```

同时引入全局样式：

```ts
import '@/styles/index.scss'
import 'element-plus/dist/index.css'
import 'virtual:uno.css'
```

**主题防闪烁（关键）**：在 `index.html` 的 `<head>` 内联脚本，在 Vue 挂载前读取 `localStorage.app-theme`，决定是否给 `<html>` 加 `.dark`，避免首屏白闪：

```html
<script>
  ;(function () {
    var t = null
    try { t = localStorage.getItem('app-theme') } catch (e) {}
    var dark = t ? t === 'dark' : true
    var el = document.documentElement
    if (dark) el.classList.add('dark')
    else el.classList.remove('dark')
  })()
</script>
```

---

## 4. 目录结构

```text
src
├─ main.ts                 # 启动入口
├─ App.vue                 # 根组件（仅 RouterView）
├─ api
│  ├─ chat.ts              # DeepSeek（OpenAI 兼容）对话
│  ├─ agent.ts             # 玻璃智能体（一问一答 / SSE 流式 / 多模态上传）
│  └─ login.ts             # 登录（占位）
├─ router/index.ts         # 路由定义
├─ store
│  ├─ helper.ts            # createPinia
│  ├─ index.ts             # initStore + 导出
│  └─ modules
│     ├─ app.ts            # 主题 / 后端 / 侧边栏 / token
│     └─ chat.ts           # 会话、消息、上下文开关
├─ views
│  ├─ chat
│  │  ├─ index.vue         # 消息区 + 输入区 + 附件上传 + 双后端流式
│  │  ├─ hooks             # useChat / useReadStream / useScroll
│  │  ├─ layout            # Layout、sider、header
│  │  └─ components/Message # 消息、富文本、图表
│  ├─ login.vue            # 登录页
│  └─ exception            # 404 / 500 异常页
├─ components              # 全局通用组件
├─ utils
│  ├─ request              # axios / fetch / eventSource 封装
│  ├─ storage/index.ts     # 带过期时间的 localStorage
│  ├─ chartTheme.ts        # ECharts 主题叠加
│  ├─ exportHtml.ts        # AI 回复导出为自包含 HTML
│  └─ copy.ts              # 复制剪贴板
├─ types                   # 全局类型声明
└─ styles                  # 全局样式（主题变量 / 玻璃拟态）
```

---

## 5. 基础能力层

### 5.1 本地存储封装（带过期时间）

`src/utils/storage/index.ts` 通过工厂函数 `createLocalStorage` 生成带过期时间的存储实例：

```ts
function set<T>(key, data) {
  const storageData = { data, expire: expire !== null ? Date.now() + expire * 1000 : null }
  window.localStorage.setItem(key, JSON.stringify(storageData))
}

function get(key) {
  const json = window.localStorage.getItem(key)
  if (json) {
    const { data, expire } = JSON.parse(json)
    if (expire === null || expire >= Date.now()) return data
  }
  remove(key)   // 过期自动清除
  return null
}
```

导出两个实例：

- `ls`：默认 7 天过期，用于主题、后端类型等长期偏好。
- `ss`：`expire: null` 永不过期，用于会话数据、侧边栏偏好、token。

> 数据结构统一为 `{ data, expire }`，内部已做 `JSON.parse` 的 `try/catch` 容错。

### 5.2 网络请求层

按场景拆成三套请求方式：

**（1）axios 实例（`utils/request/index.ts`）**——普通接口（登录等）：

```ts
const service = axios.create({ baseURL: import.meta.env.VITE_APP_BASE_API, timeout: 50000 })

// 请求拦截器：非 no-auth 接口自动注入 Bearer
service.interceptors.request.use((config) => {
  if (config.headers.Authorization !== 'no-auth') {
    config.headers.Authorization = `Bearer ${import.meta.env.VITE_APP_API_KEY}`
  } else {
    delete config.headers.Authorization
  }
  return config
})
```

**（2）fetch 封装（`utils/request/fetch.ts`）**——DeepSeek 流式（需拿到原始 `Response` 以便 `getReader()`）：

```ts
export default function fetchRequest(url, options) {
  const baseURL = import.meta.env.VITE_APP_BASE_API
  const headers = {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${import.meta.env.VITE_APP_API_KEY}`,
  }
  options.headers = { ...headers, ...options.headers }
  return fetch(baseURL + url, options)   // 返回原始 Response
}
```

**（3）玻璃智能体（`api/agent.ts`）**——服务无鉴权，直接使用原生 `fetch` 走 `/agent` 代理：

```ts
export function agentChatStream(q, threadId, signal) {
  const url = `/agent/api/chat/stream?q=${encodeURIComponent(q)}&thread_id=${encodeURIComponent(threadId)}`
  return fetch(url, { signal })
}
```

### 5.3 全局类型定义

`src/types/chat.d.ts` 用 `declare namespace Chat` 定义全局命名空间类型，无需 import 即可使用：

```ts
declare namespace Chat {
  interface AttachmentFile {   // 用户消息附带的上传文件元数据
    name: string               // 文件名
    size?: number              // 文件大小（字节）
  }
  interface Chat {
    dateTime?: string
    text?: string          // 正文
    reasoning?: string     // 思考过程
    reasoningTime?: string // 思考过程时间戳
    intent?: string        // 意图标签
    chart?: any            // 图表配置
    inversion?: boolean    // 是否用户消息
    error?: boolean
    loading?: boolean
    images?: string[]        // 用户消息附带的图片（缩略 dataURL）
    files?: AttachmentFile[] // 用户消息附带的文档附件
  }
  interface History { title?: string; isEdit: boolean; uuid: number; visible: boolean }
  interface ChatState { active: number | null; usingContext: boolean; history: History[]; chat: { uuid: number; data: Chat[] }[] }
  interface ConversationMessage { role: string; content: string }
  interface ConversationResponse { model: string; messages: ConversationMessage[]; stream: boolean; stream_options: { include_usage: boolean } }
}
```

---

## 6. 状态管理（Pinia）

### 6.1 应用状态 store（`store/modules/app.ts`）

状态字段：

```ts
state: () => ({
  siderCollapsed: ss.get('siderCollapsedApp') ?? false,  // 用户手动偏好（持久化）
  siderAutoCollapsed: false,                              // 视口自动收起（内存）
  mobileSiderOpen: false,                                 // 移动端抽屉（内存）
  theme: resolveInitTheme(),                              // light / dark
  backend: ls.get('chat-backend') ?? 'agent',             // deepseek / agent
  token: ss.get('tokenApp'),
})
```

**核心 getter**：

```ts
siderCollapsedEffective: (state) => state.siderCollapsed || state.siderAutoCollapsed
```

> 「实际是否收起」= 用户偏好 **或** 视口自动收起，任一为 true 即收起。

**核心 actions**：

```ts
// 用户手动切换：基于当前实际状态取反，并解除视口自动收起
toggleSiderCollapsed() {
  const effective = this.siderCollapsed || this.siderAutoCollapsed
  this.siderAutoCollapsed = false
  this.setSiderCollapsed(!effective)
}

// 视口驱动：窗口变窄自动收起，变宽回落到用户偏好
applyViewportSider(narrow: boolean) {
  this.siderAutoCollapsed = narrow
}

setTheme(theme) {
  this.theme = theme
  applyTheme(theme)          // 给 <html> 加/删 .dark
  ls.set('app-theme', theme)
}
```

`applyTheme` 是纯函数，通过操作 `<html>` 的 class 切换主题：

```ts
export function applyTheme(theme) {
  const el = document.documentElement
  if (theme === 'dark') el.classList.add('dark')
  else el.classList.remove('dark')
}
```

### 6.2 会话状态 store（`store/modules/chat.ts`）

状态初始化时从 `localStorage` 恢复，未命中则用默认值：

```ts
state: () => {
  const localState = ss.get('chatStore')
  return {
    ...{ history: [], active: 1002, chat: [{ uuid: 1002, data: [] }], usingContext: true },
    ...localState,
  }
}
```

核心 getter：

```ts
getChatByUuid(state) {
  return (uuid?: number) => {
    if (uuid) return state.chat.find((item) => item.uuid === uuid)?.data ?? []
    return state.chat.find((item) => item.uuid === state.active)?.data ?? []
  }
}
```

核心 actions 与职责：

| action | 职责 |
|---|---|
| `addHistory` | 新建会话（unshift 到最前 + 切 active + 跳路由） |
| `deleteHistory` | 删除会话，删除后若为当前会话则回到默认 |
| `updateHistory` | 更新会话标题 / 编辑态 |
| `setActive` | 切换当前会话并跳路由 |
| `addChatByUuid` | 向指定会话追加一条消息 |
| `updateChatByUuid` | 更新某条消息（流式增量拼接） |
| `recordState` | 持久化 `this.$state` 到 `localStorage` |
| `scheduleRecord` | 防抖持久化（500ms 合并流式高频写入） |

**流式增量的关键实现（`updateChatByUuid`）**：

```ts
// 思考过程增量：拼接上一次 reasoning + 本次 reasoning
if (chat.reasoning) {
  chat.reasoning = this.chat[chatIndex].data[index].reasoning! + chat.reasoning
}
// 正文增量：拼接上一次 text + 本次 text
if (chat.text) {
  chat.text = this.chat[chatIndex].data[index].text! + chat.text
}
const newChat = { ...this.chat[chatIndex].data[index], ...chat }
this.chat[chatIndex].data[index] = newChat
this.recordState()
```

> 每次流式帧到达时，把新片段拼到旧内容后面再整体写回，实现「逐字输出」。

**持久化防抖（`scheduleRecord`）**：流式输出期间帧到达频率很高，若每条 chunk 都全量 `JSON.stringify` 整个 store 到 localStorage 会明显卡顿。因此流式增量走 `scheduleRecord`（500ms 合并写一次），普通操作（新增 / 删除 / 切会话）走 `recordState` 立即写。

---

## 7. 路由与布局

### 7.1 路由（`router/index.ts`）

```ts
const routes = [
  {
    path: '/',
    name: 'Root',
    component: ChatLayout,
    redirect: '/chat',
    children: [
      { path: '/chat/:uuid?', name: 'Chat', component: () => import('@/views/chat/index.vue') },
    ],
  },
  { path: '/login', name: 'Login', component: () => import('@/views/login.vue') },
  { path: '/404', name: '404', component: () => import('@/views/exception/404/index.vue') },
  { path: '/:pathMatch(.*)*', name: 'notFound', redirect: '/404' },
]
```

要点：

- `/chat/:uuid?` 的 `uuid` 可选，用来定位具体会话；无 uuid 时走默认会话。
- 使用 `createWebHistory` + `scrollBehavior` 回到顶部。

### 7.2 布局（`layout/Layout.vue`）

布局结构为「背景层 + 可选移动端顶栏 + 侧边栏 + 主内容区」：

```html
<div class="app-layout h-full flex flex-col relative overflow-hidden">
  <div class="app-backdrop" />                          <!-- 玻璃背景 -->
  <header v-if="isMobile" class="mobile-topbar">...</header>  <!-- 移动端顶栏 -->
  <div class="flex flex-1 app-body">
    <Sider v-if="!isMobile" />                          <!-- 桌面端固定侧边栏 -->
    <div v-else class="mobile-sider">...</div>          <!-- 移动端抽屉侧边栏 -->
    <div class="flex-1 chat-main">
      <div class="glass-strong chat-frame">
        <RouterView v-slot="{ Component, route }">
          <component :is="Component" :key="route.fullPath" />
        </RouterView>
      </div>
    </div>
  </div>
</div>
```

移动端判断：

```ts
const isMobile = useMediaQuery('(max-width: 767px)')
```

---

## 8. 侧边栏模块

侧边栏（`layout/sider/index.vue`）支持三种形态：**桌面展开态（320px）**、**桌面收起态（68px 迷你轨道）**、**移动端抽屉**。

### 8.1 桌面端收起 / 展开

**实现步骤：**

1. 用两个计算属性决定展示哪一套 UI：

```ts
const collapsed = computed(() => appStore.siderCollapsedEffective)
const showExpanded = computed(() => isMobile.value || !collapsed.value)   // 展开态
const showCollapsed = computed(() => !isMobile.value && collapsed.value)  // 收起态
```

2. 根容器宽度随 `collapsed` 切换：

```html
<div class="sider-root transition-all duration-300"
     :class="{
       'w-68px': !isMobile && collapsed,
       'w-320px': !isMobile && !collapsed,
       'w-full': isMobile,
     }">
```

3. 展开态面板与收起态迷你轨道都用 `<Transition>` 包裹，做淡入淡出动画：

```html
<Transition name="slide-fade">
  <aside v-show="showExpanded" class="w-320px ...">...</aside>
</Transition>
<Transition name="slide2-fade">
  <aside v-show="showCollapsed" class="w-68px ...">...</aside>
</Transition>
```

4. 点击按钮触发 `changeCollapsed`：

```ts
function changeCollapsed() {
  if (isMobile.value) appStore.closeMobileSider()
  else appStore.toggleSiderCollapsed()
}
```

5. 视口自适应（在 `Layout.vue`）：窗口变窄到阈值以下自动收起，见「关键踩坑点」章节的修复方案。

### 8.2 移动端抽屉

移动端 `<768px` 时：

- `Layout.vue` 用 `v-if="!isMobile"` 隐藏固定侧边栏，改渲染抽屉。
- 抽屉通过 `mobile-sider.is-open` 控制滑入/滑出：

```css
.mobile-sider-drawer {
  width: min(85vw, 320px);
  transform: translateX(-100%);
  transition: transform 0.3s ease;
}
.mobile-sider.is-open .mobile-sider-drawer { transform: translateX(0); }
```

- 遮罩点击关闭：

```html
<div class="mobile-sider-mask" @click="appStore.closeMobileSider()" />
```

- 选择会话后自动关闭（`sider/index.vue` 内 watch）：

```ts
watch(() => chatStore.active, () => {
  if (isMobile.value) appStore.closeMobileSider()
})
```

### 8.3 主题切换

`ThemeToggle.vue` 实现「白天/黑夜」滑块开关：

- 通过 `appStore.toggleTheme()` 切换主题。
- `knob` 玻璃球用 `transform: translateX(100%)` 在黑白两种状态间滑动，太阳/月亮图标通过 `opacity` + `rotate` 翻转显隐。

---

## 9. 会话管理

会话列表在 `layout/sider/List.vue`，操作对应 `chat.ts` store。

**新建会话**：

```ts
// store
createNewChat() {
  this.active = 1002
  this.reloadRoute('')
}
```

`handleSubmit` 中，当路由没有 uuid 时先建会话：

```ts
if (!+uuid || +uuid === 0) {
  chatStore.addHistory({ title: '新建会话', uuid: Date.now(), isEdit: false, visible: false })
  ss.set('chatSubmitPrompt', prompt.value)   // 暂存输入，跳转后自动补发
  return
}
```

**选择会话**：

```ts
async function handleSelect({ uuid }) {
  if (isActive(uuid)) return
  if (chatStore.active) chatStore.updateHistory(chatStore.active, { isEdit: false })
  await chatStore.setActive(uuid)
}
```

**删除会话**（悬浮「更多」按钮 + 虚拟触发 Popover）：

```html
<el-popover virtual-triggering :visible="visible" :virtual-ref="buttonRef">
  <el-button link type="danger" @click="handleDelete">删除该会话</el-button>
</el-popover>
```

**标题自动生成**：首次消息发送后，用消息前 50 字符作为标题。

---

## 10. 消息发送与双后端流式

核心逻辑在 `views/chat/index.vue`。

**整体发送流程（`onConversation`）：**

```ts
async function onConversation() {
  if (loading.value) return
  const message = prompt.value.trim()
  const pendingAtts = [...attachments.value]
  if (!message && pendingAtts.length === 0) return   // 无文字且无附件 → 不可发送

  controller = new AbortController()               // 1. 每次新建中止信号
  // 2. 组装历史上下文（开启多轮时带上）
  const conversationList = dataSources.value.filter(item => item.text).map(item => ({
    role: item.inversion ? 'user' : 'assistant',
    content: item.text,
  }))
  // 3. 图片压缩为缩略 dataURL，文档仅记元数据
  const imageThumbs = (await Promise.all(
    pendingAtts.filter(a => a.kind === 'image').map(a => compressImageToDataUrl(a.file)),
  )).filter(Boolean)
  const fileMetas = pendingAtts.filter(a => a.kind === 'file').map(a => ({ name: a.name, size: a.size }))
  // 4. 添加用户消息（文字 + 多图 + 多文件）
  addChat(+uuid, { dateTime: ..., text: message, images: imageThumbs.length ? imageThumbs : undefined, files: fileMetas.length ? fileMetas : undefined, inversion: true, error: false })
  // 5. 添加 AI 占位消息（思考中）
  addChat(+uuid, { dateTime: ..., text: '', reasoning: '', loading: true, inversion: false })
  // 6. 按后端分发：智能体带附件走 multipart 上传，纯文本走 SSE
  if (backend.value === 'agent') {
    if (pendingAtts.length) await onAgentUploadConversation(message, pendingAtts)
    else await onAgentConversation(message)
  } else {
    await onDeepseekConversation(message, conversationList)
  }
}
```

### 10.1 DeepSeek 流式（OpenAI 兼容）

**实现步骤：**

1. 组装请求体（开启上下文时携带历史）：

```ts
const paramMsg = []
if (usingContext.value) paramMsg.push(...conversationList)
paramMsg.push({ role: 'user', content: message })
```

2. 发起请求（返回原始 `Response`）：

```ts
const response = await chatCompletions(controller.signal, paramMsg)
```

其中 `api/chat.ts`：

```ts
export function chatCompletions(stream, messages) {
  return fetchRequest('/v1/chat/completions', {
    method: 'POST',
    body: JSON.stringify({
      model: 'deepseek-v4-flash',
      messages,
      stream: true,
      stream_options: { include_usage: true },
    }),
    signal: stream,
  })
}
```

3. 解析流并逐帧更新：

```ts
await handleStreamResponse(response, (jsonData) => {
  const contentText = jsonData.choices[0]?.delta?.content
  if (contentText) {
    updateChat(+uuid, dataSources.value.length - 1, { text: contentText, ... })
    scrollToBottomIfAtBottom()
  }
  const reasoning = jsonData.choices[0].delta.reasoning_content
  if (reasoning) {
    updateChat(+uuid, dataSources.value.length - 1, { reasoning, ... })
    scrollToBottomIfAtBottom()
  }
})
```

> DeepSeek 兼容接口返回 `choices[0].delta.content`（正文）和 `choices[0].delta.reasoning_content`（思维链）。

### 10.2 玻璃智能体流式（SSE 逐节点）

**实现步骤：**

1. 用 `threadId` 作为会话标识发起 SSE：

```ts
const threadId = String(chatStore.active ?? Date.now())
const response = await agentChatStream(message, threadId, controller.signal)
```

2. 每帧解析后按节点名分发处理：

```ts
await handleStreamResponse(response, (jsonData) => {
  for (const node of Object.keys(jsonData)) {
    const payload = jsonData[node] || {}
    const updates = { inversion: false, error: false, loading: true }
    if (payload.intent)       updates.intent = payload.intent        // 意图
    if (payload.final_answer) updates.text = payload.final_answer    // 最终回答
    if (payload.chart_config) updates.chart = payload.chart_config   // 图表

    // 其余字段归入「思考过程」
    const rest = {}
    for (const key of Object.keys(payload)) {
      if (['intent', 'final_answer', 'chart_config'].includes(key)) continue
      rest[key] = payload[key]
    }
    if (Object.keys(rest).length) {
      updates.reasoning = formatAgentNode(node, rest)
      updates.reasoningTime = new Date().toLocaleString()
    }
    updateChat(+uuid, dataSources.value.length - 1, updates)
  }
})
```

3. 节点名映射为中文标签：

```ts
const nodeLabels = {
  router: '路由识别',
  data_parse: '参数解析',
  data_execute: '数据执行',
  data_analyze: '数据分析',
  report_generate: '报告生成',
}
```

### 10.3 流式解析通用逻辑（`hooks/useReadStream.ts`）

这是流式读取核心，处理「跨 chunk 的半包」问题：

```ts
async function handleStreamResponse(response, onMessage) {
  const reader = response.body.getReader()
  const decoder = new TextDecoder()
  let done = false
  let buffer = ''
  while (!done) {
    const { value, done: isDone } = await reader.read()
    done = isDone
    buffer += decoder.decode(value, { stream: true })
    if (!buffer.includes('\n')) continue          // 未遇到完整行，继续累积
    const messages = buffer.split('\n')
    for (const message of messages) {
      const jsonStr = message.replace(/^\s*data:\s*/, '').trim()  // 去掉 data: 前缀
      if (jsonStr === '[DONE]') { done = true; continue }
      if (jsonStr === '') continue
      try {
        const jsonData = JSON.parse(jsonStr)
        onMessage(jsonData)
      } catch (error) { console.error('Error parsing JSON:', error) }
    }
    buffer = ''                                     // 清空缓冲
  }
}
```

**关键技术点**：`reader.read()` 返回的是二进制 chunk，可能把一个 JSON 帧拆成多个 chunk。用 `buffer` 累积 + `\n` 作为行分隔符判断完整帧，确保不会解析到半包 JSON。

### 10.4 中断与错误处理

```ts
// 中断
function handleStop() {
  if (loading.value) {
    controller.abort()
    loading.value = false
  }
}

// 统一错误处理
function handleStreamError(error) {
  loading.value = false
  if (error instanceof DOMException && error.name === 'AbortError') {
    updateChat(+uuid, dataSources.value.length - 1, { text: '\n\n用户手动中断。', loading: false })
  } else {
    updateChat(+uuid, dataSources.value.length - 1, { text: '\n\n服务器异常，请稍后再试。', error: true, loading: false })
  }
}
```

### 10.5 多模态附件（图片 + 文档）

玻璃智能体支持在输入框上传多张图片与多个文档，与文字一起发送（DeepSeek 后端不支持附件）。

**（1）附件上传与归类**

输入区两个隐藏 `<input type="file">` 分别负责图片（`image/jpeg,png,webp,bmp,gif`）与文档（`.md/.txt/.pdf/.docx/.pptx/.xlsx`），选择后按扩展名归类：

```ts
function classifyFile(file) {
  const extDot = `.${(file.name.split('.').pop() || '').toLowerCase()}`
  if (IMAGE_EXT_SET.has(extDot) || file.type.startsWith('image/')) return 'image'
  if (DOC_EXT_SET.has(extDot)) return 'file'
  return null   // 不支持格式 → ElMessage.warning 提示
}
```

图片生成本地预览 URL（`URL.createObjectURL`），文档记录元数据，统一进入 `attachments` 列表在输入框上方预览。

**（2）发送时的图片压缩**

为避免把原图塞进 localStorage 导致容量溢出，发送前把图片压缩为缩略 dataURL（长边上限 1024、JPEG 质量 0.82）：

```ts
function compressImageToDataUrl(file, maxSide = 1024, quality = 0.82) {
  // FileReader → Image → canvas 缩放 → toDataURL('image/jpeg')
}
```

文档只记元数据 `{ name, size }`，不存文件本体。

**（3）消息气泡展示**

用户消息携带 `images`（缩略图，`el-image` 可点击放大）与 `files`（文档卡片：图标 + 文件名 + 大小）；若只发图片 / 文档而未输入文字，则不渲染文字气泡，仅展示附件。

**（4）多模态上传（multipart）**

附件场景走 `agentChatUpload`（POST `/agent/api/chat/upload`），FormData 携带 `message`、`thread_id`、多张 `images`、多个 `files`，一次性返回 `{ intent, answer, chart }`；纯文本仍走 SSE 流式。

```ts
export function agentChatUpload(message, threadId, { images, files }, signal) {
  const fd = new FormData()
  fd.append('message', message)
  fd.append('thread_id', threadId)
  images.forEach(f => fd.append('images', f, f.name))
  files.forEach(f => fd.append('files', f, f.name))
  return fetch('/agent/api/chat/upload', { method: 'POST', body: fd, signal })
}
```

**（5）发送按钮可用条件**

```ts
const canSend = computed(() => !loading.value && (prompt.value.trim() !== '' || attachments.value.length > 0))
```

输入了文字 **或** 已添加附件即可发送；切换后端时若仍有未发送附件会提示先处理。

---

## 11. 富文本渲染

富文本渲染在 `components/Message/Text.vue`，基于 `markdown-it` 插件链。

### 11.1 Markdown

初始化 `markdown-it` 实例：

```ts
const mdi = new MarkdownIt({
  html: false,          // 禁用原始 HTML，防 XSS
  linkify: true,
  highlight(code, language) {
    const validLang = !!(language && hljs.getLanguage(language))
    if (validLang) return highlightBlock(hljs.highlight(code, { language }).value, language)
    return highlightBlock(hljs.highlightAuto(code).value, '')
  },
})

mdi
  .use(MdLinkAttributes, { attrs: { target: '_blank', rel: 'noopener' } })  // 外链新窗口
  .use(MdKatex)    // KaTeX 数学公式
  .use(MdMermaid)  // Mermaid 流程图
```

渲染前先做转义处理，避免 `$` 数字、`\[` 括号被误解析：

```ts
const textVal = computed(() => {
  const value = props.text ?? ''
  if (!props.asRawText) {
    const escapedText = escapeBrackets(escapeDollarNumber(value))
    return mdi.render(escapedText)   // 输出 HTML，用 v-html 绑定
  }
  return value
})
```

- `escapeDollarNumber`：把 `$` 后紧跟数字的情况转义为 `\$`，防止价格/金额被当成公式。
- `escapeBrackets`：把 `\[ ... \]`、`\( ... \)` 转成 `$$ ... $$`、`$ ... $`，兼容 KaTeX。

### 11.2 代码高亮与一键复制

`highlightBlock` 生成带「复制代码」按钮的代码块结构：

```ts
function highlightBlock(str, lang) {
  return `<pre class="code-block-wrapper">
    <div class="code-block-header">
      <span class="code-block-header__lang">${lang}</span>
      <span class="code-block-header__copy">复制代码</span>
    </div>
    <code class="hljs code-block-body ${lang}">${str}</code>
  </pre>`
}
```

复制事件通过 `onUpdated` 钩子，在内容每次更新后重新绑定点击事件：

```ts
onUpdated(() => addCopyEvents())
```

复制逻辑用 `utils/copy.ts`（创建临时 `textarea` + `execCommand('copy')`）。

### 11.3 LaTeX 数学公式（KaTeX）

通过 `@vscode/markdown-it-katex` 插件解析 `$...$`、`$$...$$`，并引入 `katex/dist/katex.min.css` 样式。

### 11.4 Mermaid 流程图

通过 `mermaid-it-markdown` 插件解析 ` ```mermaid ` 代码块并渲染为 SVG。

### 11.5 思考过程（Reasoning）展示

AI 消息的思考过程折叠组件：

```html
<div v-if="reasoning" class="reasoning-wrap">
  <button class="reasoning-toggle" @click="changeShowReasoning">
    <span class="reasoning-label">深度思考</span>
    <span class="reasoning-status">
      <span v-if="loading" class="thinking-dots"><i></i><i></i><i></i></span>
      <svg v-else class="reasoning-chevron">...</svg>
    </span>
  </button>
  <div v-if="showReasoning" class="reasoning-body markdown-body" v-html="reasoningVal" />
</div>
```

- `showReasoning` 默认展开，点击折叠/展开。
- `loading` 时显示跳动圆点动画。
- 思考过程正文同样走 `markdown-it` 渲染，视觉上用「X 光网格纹理」区分。

---

## 12. 图表与看板（ECharts）

图表渲染在 `components/Message/Chart.vue`，兼容「单图」和「看板（dashboard）」两种结构。

### 12.1 结构判断

```ts
const isDashboard = computed(() => props.option?.layout === 'dashboard')
const charts = computed(() => props.option?.charts || [])
```

- 无 `layout` 字段 → 单图，直接用 `props.option` 作为 ECharts option。
- `layout === 'dashboard'` → 看板：`title` + `kpis`（KPI 卡片）+ `charts`（多张子图）。

### 12.2 看板子图 span 解析

```ts
function chartSpan(c) {
  return c && typeof c === 'object' && c.option != null ? Number(c.span) || 12 : 12
}
```

`span = 6` 半行（两两并排）、`span = 12` 整行。子图项兼容 `{ option, span }` 包装与旧版纯 option。

### 12.3 渲染流程

```ts
async function render() {
  await nextTick()
  disposeAll()          // 先销毁旧实例，避免重复 init 泄漏

  if (isDashboard.value) {
    charts.value.forEach((c, i) => {
      const el = els.value[i]
      if (!el) return
      const inst = echarts.init(el)
      const opt = c.option != null ? c.option : c
      if (opt) inst.setOption(withChartStyle(opt), true)
      subCharts.push(inst)
    })
  } else {
    singleChart = echarts.init(chartRef.value)
    singleChart.setOption(withChartStyle(props.option), true)
  }
}

watch(() => props.option, render, { deep: true })
```

- `withChartStyle`（`utils/chartTheme.ts`）以「缺省补齐」方式叠加统一视觉风格，不覆盖后端已配置的数值/格式化器。
- `window.resize` 时统一 `resize()` 所有图表实例。

### 12.4 KPI 卡片格式化

```ts
function formatValue(v) {
  if (v === null || v === undefined) return '—'
  if (typeof v === 'number') return v.toLocaleString('zh-CN', { maximumFractionDigits: 2 })
  return String(v)
}
```

### 12.5 AI 回复下载导出

每条 AI 回复下方有「下载所生成的内容」按钮，把该条回复（markdown 正文 + 图表看板）打包为自包含 HTML，浏览器离线可直接打开，图表保留交互。

**实现（`utils/exportHtml.ts` + `Message/index.vue`）：**

1. 收集正文 HTML：`Text.vue` 暴露 `getRenderedTextHtml()`，返回 markdown 渲染后的正文。
2. 收集图表数据：`Chart.vue` 暴露 `getExportData()`，返回 `{ title, kpis, charts }`。
3. 生成图表容器：`buildChartHtml` 把每个图表 option 内嵌到 `data-echarts` 属性，配合 ECharts CDN 脚本在打开时动态 `echarts.init + setOption`，保留 tooltip / 缩放交互。
4. 组装文档：`buildExportHtml(title, body, hasChart)` 内联 CSS、去掉代码块「复制代码」头部，按需注入 ECharts CDN。
5. 触发下载：`downloadHtml` 用 `Blob + URL.createObjectURL + <a download>` 保存。

```ts
function handleDownload() {
  const html = textRef.value?.getRenderedTextHtml?.() || ''
  const data = chartRef.value?.getExportData?.()
  const chartHtml = buildChartHtml(data)
  const body = html + chartHtml
  if (!body) return
  const hasChart = !!(data && data.charts && data.charts.length)
  const title = (props.text || '').slice(0, 30)
  downloadHtml(`AI回复_${formatTimestamp()}.html`, buildExportHtml(title, body, hasChart))
}
```

---

## 13. 滚动控制

滚动逻辑在 `hooks/useScroll.ts`，基于 Element Plus 的 `ScrollbarInstance`。

**核心方法：**

| 方法 | 说明 |
|---|---|
| `scrollToBottom` | 立即滚动到底（`nextTick` 后 `setScrollTop(scrollHeight)`） |
| `scrollToBottomIfAtBottom` | 仅当用户已在底部附近（距离 ≤100px）才自动跟随，避免流式输出时打断用户上翻 |
| `scrollToTopAnimated` / `scrollToBottomAnimated` | 平滑动画滚动（`requestAnimationFrame` + `easeInOutCubic`） |

**平滑滚动实现：**

```ts
const easeInOutCubic = (t) => t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2
const step = (now) => {
  const progress = Math.min((now - startTime) / duration, 1)
  wrap.scrollTop = start + change * easeInOutCubic(progress)
  if (progress < 1) rafId = requestAnimationFrame(step)
  else resolve()
}
```

**`scrollToBottomIfAtBottom` 的关键价值**：流式输出时，只有用户停留在底部才自动跟随滚动；一旦用户上翻查看历史，就不再强制滚动，提升体验。

---

## 14. 主题系统（玻璃拟态）

主题在 `styles/theme.scss` 中定义，通过 CSS 变量 + `<html>.dark` class 切换。

### 14.1 双主题变量

```css
:root {
  --brand: #00a8e8;
  --brand-2: #0077b6;
  --text-primary: #2c3e50;
  --glass-bg: rgba(255, 255, 255, 0.62);
  --bubble-user: linear-gradient(135deg, #00a8e8 0%, #0077b6 100%);
  /* ... */
  color-scheme: light;
}

html.dark {
  --brand: #7b61ff;
  --brand-2: #b48cff;
  --text-primary: #eaeaea;
  --glass-bg: rgba(30, 34, 45, 0.55);
  /* ... */
  color-scheme: dark;
}
```

### 14.2 玻璃拟态

通用玻璃工具类：

```css
.glass {
  background-color: var(--glass-bg);
  backdrop-filter: blur(18px) saturate(150%);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow);
}
.glass-strong {
  background-color: var(--glass-bg-strong);
  backdrop-filter: blur(24px) saturate(170%);
  border: 1px solid var(--glass-border);
}
```

### 14.3 背景光晕

`.app-backdrop` 用多个径向渐变 + 漂浮光斑（`filter: blur` + 动画）营造纵深，供毛玻璃「透」出色彩。

### 14.4 与 Element Plus 联动

把品牌色映射到 Element Plus 的 CSS 变量：

```css
:root {
  --el-color-primary: var(--brand);
  --el-color-primary-light-3: color-mix(in srgb, var(--brand) 70%, white);
  --el-bg-color: var(--glass-bg);
  --el-text-color-primary: var(--text-primary);
}
```

---

## 15. 移动端适配

断点：`<768px`（`useMediaQuery('(max-width: 767px)')`）。

| 场景 | 处理 |
|---|---|
| 侧边栏 | 固定侧边栏替换为抽屉（滑入 + 遮罩） |
| 顶部栏 | 显示 `mobile-topbar`（菜单按钮 + 标题） |
| 聊天标题 | 隐藏桌面端标题栏 |
| 气泡宽度 | `max-width: 92%` |
| 图表高度 | 320px → 240px |
| 内边距 | 聊天区、输入区收紧 |

---

## 16. 关键踩坑点与优化

### 16.1 侧边栏「点了收起又立刻弹回」的修复

**问题现象**：窗口宽度卡在断点附近时，点击收起/展开侧边栏会「弹回来」，要点很多次。

**根因**：侧边栏自动收起原由 `matchMedia('(max-width: 992px)')` 的 `change` 事件驱动。当窗口宽度接近 992px 时，浏览器垂直滚动条随消息内容增减而出现/消失（约 15px），使可用宽度在断点两侧反复横跳，`change` 事件反复触发，自动收起状态来回覆盖用户手动操作。

**修复方案（`Layout.vue`）**：

1. 采用「滞后区间」：低于 `960px` 才自动收起，高于 `1024px` 才自动展开，`960~1024` 之间保持现状。

```ts
const SIDER_NARROW_BREAKPOINT = 960
const SIDER_WIDE_BREAKPOINT = 1024
let lastNarrow: boolean | null = null

function syncViewport() {
  const width = document.documentElement.clientWidth
  let next: boolean | null = null
  if (width < SIDER_NARROW_BREAKPOINT) next = true
  else if (width > SIDER_WIDE_BREAKPOINT) next = false
  if (next !== null && next !== lastNarrow) {
    lastNarrow = next
    appStore.applyViewportSider(next)
  }
}
```

2. 用 `requestAnimationFrame` 去抖 `resize` 事件：

```ts
function onResize() {
  cancelAnimationFrame(rafId)
  rafId = requestAnimationFrame(syncViewport)
}
```

3. 首屏立即同步一次视口，避免展开状态闪烁。

### 16.2 流式半包问题

SSE chunk 可能把一条 JSON 帧拆开，用 `buffer` 累积 + `\n` 分隔判断完整帧（见 10.3）。

### 16.3 主题防闪烁

在 `index.html` 内联脚本提前设置 `.dark`，避免 Vue 挂载前白屏闪烁（见第 3 章）。

### 16.4 XSS 防护

`markdown-it` 配置 `html: false`，禁用原始 HTML 注入。

---

## 17. 本地存储键位

| key | 存储 | 内容 |
|---|---|---|
| `chatStore` | localStorage（`ss`） | 会话历史与消息数据 |
| `app-theme` | localStorage（`ls`） | 主题：`light` / `dark` |
| `chat-backend` | localStorage（`ls`） | 当前后端：`deepseek` / `agent` |
| `siderCollapsedApp` | localStorage（`ss`） | 侧边栏折叠偏好 |
| `tokenApp` | localStorage（`ss`） | 登录 token |
| `chatSubmitPrompt` | localStorage（`ss`） | 新建会话时暂存的待发送输入 |

---

## 18. 接口约定

### 18.1 DeepSeek（`src/api/chat.ts`）

- `chatCompletions(stream, messages)`：POST `/v1/chat/completions`，`model=deepseek-v4-flash`、`stream=true`。
- 支持 `choices[].delta.content`（正文）与 `choices[].delta.reasoning_content`（思维链）。

### 18.2 玻璃智能体（`src/api/agent.ts`）

| 接口 | 说明 |
|---|---|
| `agentChat(message, threadId)` | 一问一答（非流式），返回 `{ intent, answer, chart }` |
| `agentChatStream(q, threadId, signal)` | SSE 流式，逐节点推送，`[DONE]` 结束 |
| `agentChatUpload(message, threadId, { images, files }, signal)` | 多模态 multipart 上传（图片 + 文档），非流式返回 `{ intent, answer, chart }` |

流式帧字段约定：

| 字段 | 含义 |
|---|---|
| `intent` | 意图标签：`qa` 知识问答 / `data` 数据分析 / `chat` 闲聊 |
| `final_answer` | 最终回答（Markdown 正文） |
| `chart_config` | ECharts 图表配置（`layout=dashboard` 时为 KPI + 多子图看板） |
| 其余字段 | 归入「思考过程」：`router`、`data_parse`、`data_execute`、`data_analyze`、`report_generate` |

---

## 总结

本项目前端采用「Vue 3 + TypeScript + Vite + Pinia + Element Plus + UnoCSS」标准技术栈，围绕 AI 对话场景做了以下关键设计：

1. **双后端统一流式模型**：DeepSeek（OpenAI 兼容）与玻璃智能体（SSE 逐节点）共用 `useReadStream` 解析器，差异仅在字段映射。
2. **增量式消息更新**：流式帧通过 store 的「拼接式」更新实现逐字输出，并实时持久化。
3. **富文本渲染链**：markdown-it + highlight.js + KaTeX + Mermaid，覆盖代码、公式、流程图。
4. **玻璃拟态主题**：CSS 变量 + `.dark` class + 防闪烁初始化，实现无闪切换。
5. **响应式与可访问性**：滞后区间 + rAF 去抖解决侧边栏回弹，移动端抽屉适配，`prefers-reduced-motion` 降低动画。


